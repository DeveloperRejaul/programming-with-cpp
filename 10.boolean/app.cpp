#include <iostream>
using namespace std;


int main(int argc, char const *argv[])
{
    // bool isCodingFun = true;
    // bool isFishTasty = false;

    // cout << isCodingFun << "\n";  // Outputs 1 (true)
    // cout << isFishTasty << "\n"; // Outputs 0 (false)


    // boolalpha is a manipulator in C++ that allows you to print boolean values as "true" or "false" instead of 1 or 0. When you use boolalpha, it changes the way boolean values are displayed in the output stream.
    // bool isCodingFun = true;
    // bool isFishTasty = false;

    // cout << boolalpha; // enable printing "true"/"false"

    // cout << isCodingFun << "\n";   // Outputs true
    // cout << isFishTasty << "\n";  // Outputs false


    // Resetting Back With noboolalpha
    // bool isCodingFun = true;

    // cout << boolalpha;   // print as true/false
    // cout << isCodingFun << "\n";  // Outputs true

    // cout << noboolalpha; // reset to 1/0
    // cout << isCodingFun << "\n";  // Outputs 1

    // Boolean Expressions
    int x = 10;
    int y = 9;
    cout << (x > y); // returns 1 (true), because 10 is higher than 9

    cout << (10 > 9); // returns 1 (true), because 10 is higher than 9
    cout << (x == 10); // returns 1 (true), because x is equal to 10


    /* code */
    return 0;
}
